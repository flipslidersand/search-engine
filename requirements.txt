# Phase 1 (MVP) は stdlib (sqlite3) のみで動作する。
# 形態素解析の精度を上げる場合は以下を導入（任意）:
# fugashi[unidic-lite]   # 軽量な MeCab バインディング
# ja-ginza               # GiNZA + SudachiPy（高精度・NER同時）

# Phase 2（ベクトル検索）:
# sentence-transformers
# faiss-cpu

# Phase 2（ベクトル検索）フォールバック用:
numpy>=1.26

# Phase 3（Web API サーバー）:
fastapi>=0.110
uvicorn[standard]>=0.29

# Phase 4（RAG + Ollama 連携）:
httpx>=0.27

# Phase 4（MCP サーバー）:
mcp>=2.0

# Phase 5（Qdrant ベクトルストア）:
# qdrant-client>=1.9

# Phase 6（観測点）:
prometheus-client>=0.20
